# Ingestion and context

Keep SEC ingestion/chunking/index freshness separate from viewer acquisition.
Follow the documented rechunk -> table chunks -> embeddings -> index sequence
when a separately authorized ingestion task occurs. Comparative decomposition,
context rendering, numeric units, and prompt-grounding contracts must preserve
the same evidence identity sent to generation and evaluation.
