# Measurement and attribution

Keep client, network, server, provider, retrieval-stage, reranker, rendering,
and persistence measurements separate. Use identical workloads for comparison,
report percentiles with sample counts, and inspect first useful content rather
than only total completion.
