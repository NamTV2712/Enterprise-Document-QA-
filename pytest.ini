[pytest]
testpaths = tests
addopts = -m "not live_network"
markers =
    integration: touches real local services or fixtures outside the hermetic default suite
    live_network: requires internet access (SEC, Hugging Face, Groq, Qdrant Cloud); opt-in only and deselected by default
