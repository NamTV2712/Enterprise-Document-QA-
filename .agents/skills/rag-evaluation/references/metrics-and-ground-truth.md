# Metrics and ground truth

Use Recall@K, Precision@K, MRR, and NDCG only with suitable labelled retrieval
ground truth. Keep deterministic keyword coverage named as a proxy. Keep
faithfulness, factual correctness, relevance, citation-number validity, and
claim support separately defined and separately reported.
