# Representations and coverage

Normalized, structured, indexed-only, canonical HTML, and PDF representations
have separate availability and revision states. Verify headings, prose,
tables, code-point offsets, and search coverage. Do not call a structured
representation complete when supported prose is omitted or search disagrees
with the normalized source without an explicit limitation.
