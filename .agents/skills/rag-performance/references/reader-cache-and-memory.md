# Reader cache and memory

Inspect TTLs, in-flight deduplication, consumer cancellation, document-list
retention, persistence writes, event listeners, Markdown work, and repeated
open/close cycles. Do not claim a blanket bounded-cache property when an
implementation map is unbounded or transport cancellation is shared.
