# Identity and revisions

Use chunk ID first, then verified document and metadata identity. Preserve
accession, CIK, primary/companion role, source-set revision, document revision,
hash, answer variant, and historical snapshot. Duplicate exact matches are
ambiguous; stale revisions must be visible conflicts.
