# Diagnosis and regressions

Use a fixed query set with fresh chunks/embeddings/indexes where required.
Compare one change at a time, slice by ticker/section/query type, inspect worst
failures, and preserve a regression case for the original failure. Keep quality
and latency results in separate records.
