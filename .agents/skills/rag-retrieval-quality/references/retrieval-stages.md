# Retrieval stages

Record query, scope, corpus revision, candidate IDs, stage scores/ranks,
fusion inputs/outputs, reranker values, structured promotions, final selected
IDs, and the evidence sent to generation. State when a stage is not instrumented
instead of fabricating timing or candidate equivalence.
