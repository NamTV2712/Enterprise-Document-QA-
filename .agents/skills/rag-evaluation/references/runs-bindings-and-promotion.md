# Runs, bindings, and promotion

Record dataset revision, priorities/slices, renderer fingerprint, generation
model, judge model, provider, prompt/config, checkpoint path, completion and
skip counts, and evidence provenance. Never append a different generation
binding to an existing checkpoint. Official status requires the repository’s
documented clean-run policy and complete evidence.
